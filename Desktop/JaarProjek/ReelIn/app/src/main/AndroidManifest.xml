<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nmu.wrrv301.reelin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterPageOne"
            android:theme="@style/AppTheme"/>
        <activity android:name=".RegisterPageTwo"
            android:theme="@style/AppTheme"/>
        <activity android:name=".MainMenu"
            android:theme="@style/AppTheme"/>
        <activity android:name=".ManageAccount"
            android:theme="@style/AppTheme"/>
        <activity android:name=".ViewTags"
            android:theme="@style/AppTheme"/>
        <activity android:name=".LocationCaptured"/>
    </application>

</manifest>